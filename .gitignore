# Build artefacts
wifi-setup/dist/
wifi-setup/node_modules/
wifi-setup/.vite/
wifi-setup-api/wifi-setup-api

# Runtime-only — Pi writes this at boot, never committed
wifi-setup/public/config.json
wifi-setup/dist/config.json

# Local secrets — never commit
.env
command-signing.key

# bootstrap.sh contains embedded Tailscale OAuth secret — keep repo private
# or rotate the secret if this repo ever becomes public

# OS / editor
.DS_Store
*.swp
*.swo

# SD card images — stored in object storage, not git
images/*.img
images/*.img.gz
images/*.img.xz
